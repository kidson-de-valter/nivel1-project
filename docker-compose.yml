version: "3.9"

services:
  api:
    build: ./backend
    ports:
      - "8001:8001"
    depends_on:
      - db
  
  db: 
    image: postgres:15
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: clients
    ports:
      - "5432:5432"
